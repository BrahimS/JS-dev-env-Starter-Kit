import '../sass/index.sass';
import name from '../js/config';

const add =(a, b) => a + b;
/* eslint-disable no-console */
console.log (add (14, 20));
export default add;

/* eslint-disable no-console */
console.log (name);

